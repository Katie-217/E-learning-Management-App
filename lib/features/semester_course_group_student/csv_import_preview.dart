import 'package:flutter/material.dart';

class CsvImportPreviewPage extends StatelessWidget {
  const CsvImportPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CSV Import Preview')),
      body: const Center(child: Text('Preview & validation of CSV import')),
    );
  }
}












