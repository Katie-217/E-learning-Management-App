import 'package:flutter/material.dart';

class ManagementView extends StatelessWidget {
  const ManagementView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Semester/Course/Group/Student')),
      body: const Center(
        child: Text('CRUD + CSV Import Preview Placeholder'),
      ),
    );
  }
}



















